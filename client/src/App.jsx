import React from "react";
import { BrowserRouter, Router, Routes } from "react-router-dom";

const App = () => {
  return (
    <BrowserRouter>
      <Router>
        <Routes></Routes>
      </Router>
    </BrowserRouter>
  );
};

export default App;
